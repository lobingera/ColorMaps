# construct_from_string.jl
function construct_from_string(s::AbstractString,N::Integer)
	colormap = Array(RGB{Float64},N)
	#s1 = split(s)
	#for (i,s2) in enumerate(s1)
	colormap
end

construct_from_string(s) = construct_from_string(s,defaultN)

		